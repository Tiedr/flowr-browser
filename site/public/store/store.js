const cards=[...document.querySelectorAll('[data-kind]')],filters=[...document.querySelectorAll('[data-filter]')],search=document.querySelector('#search'),empty=document.querySelector('#empty');let kind='all';function apply(){const q=search.value.trim().toLowerCase();let n=0;cards.forEach(c=>{const show=(kind==='all'||c.dataset.kind===kind)&&(!q||c.dataset.search.includes(q)||c.innerText.toLowerCase().includes(q));c.hidden=!show;if(show)n++});empty.hidden=!!n}filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');kind=b.dataset.filter;apply()}));search.addEventListener('input',apply);
