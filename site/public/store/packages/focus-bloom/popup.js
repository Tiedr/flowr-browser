let remaining=1500,timer=null,running=false,sessions=0;const time=document.querySelector('#time'),status=document.querySelector('#status');chrome.storage.local.get(['sessions'],v=>{sessions=v.sessions||0;render()});function render(){time.textContent=`${String(Math.floor(remaining/60)).padStart(2,'0')}:${String(remaining%60).padStart(2,'0')}`;document.querySelector('#sessions').textContent=`${sessions} bloom${sessions===1?'':'s'} today`;document.body.classList.toggle('active',running)}document.querySelector('#start').onclick=()=>{running=!running;status.textContent=running?'Growing quietly…':'Paused';document.querySelector('#start').textContent=running?'Pause':'Resume';clearInterval(timer);if(running)timer=setInterval(()=>{remaining--;if(remaining<=0){clearInterval(timer);running=false;sessions++;remaining=1500;chrome.storage.local.set({sessions});status.textContent='Bloom complete — take a quiet break'}render()},1000);render()};document.querySelector('#reset').onclick=()=>{clearInterval(timer);running=false;remaining=1500;status.textContent='Ready to grow';document.querySelector('#start').textContent='Begin';render()};render();
